#pragma once

namespace Halia
{
	//! An base-class used to represent arbitrary render-system specific implementations
	class RenderCache
	{
	public:
		RenderCache( );
		virtual ~RenderCache( );
	};
};